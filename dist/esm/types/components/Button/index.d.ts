import React from 'react';
import type { ButtonProps } from './Button.types';
declare const Button: ({ type, size, disabled, primary, text, onClick, ...props }: ButtonProps) => React.JSX.Element;
export default Button;
